export const AGENT_BACKEND_URL = 'http://10.0.2.2:8000'; // Android emulator host alias

// When true, the app connects automatically on launch (used by the live logcat gate).
export const AUTO_CONNECT = false;
